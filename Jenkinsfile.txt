pipeline {
    agent any
    stages {
        stage('Gradle Build') {
          steps {
            sh 'gradle clean build'
          }
        }
        stage('Test') {
            steps {
                sh 'gradle clean test'
            }
        }
    }
}