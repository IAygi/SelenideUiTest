pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh './gradlew build'
            }
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'gradle clean test'
            }
        }
    }
}