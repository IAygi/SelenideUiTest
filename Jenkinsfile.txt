pipeline {
    agent any
    stages {
        stage('Gradle Build') {
          steps {
            sh './gradlew clean test --no-daemon'
          }
        }
        stage('Test') {
            steps {
                sh 'gradle clean test'
            }
        }
    }
}